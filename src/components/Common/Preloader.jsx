const Preloader = () => {
    return (
        <div className={`flex items-center justify-center h-full w-full grow`}>
            <div className="relative preloader animate-spin">
            </div>
        </div>
    )
}

export default Preloader;